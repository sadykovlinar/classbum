Classbum — Gamified Math Training Platform for Kids

Проект помогает детям учиться математике через игровую мотивацию и систему наград.

🚀 Архитектура проекта
classbum/
│
├── backend/              # API, логика сервера
│   ├── index.js          # точка входа
│   ├── routes/           # все HTTP-маршруты
│   ├── controllers/      # обработчики маршрутов
│   ├── services/         # бизнес-логика (генерация задач, статистика)
│   ├── middleware/       # промежуточные функции (авторизация)
│   ├── utils/            # вспомогательные функции
│   └── data/             # хранение временных данных
│
└── frontend/             
    ├── js/               # логика виджетов (auth, тренажёр)
    └── webflow-embeds/   # HTML-блоки для вставки в Webflow

🔧 Запуск backend локально
cd backend
npm install
node index.js


Backend запускается на:

http://localhost:3000


(Если порт другой — поменяй в README)

🗄️ Миграции БД

В `backend/migrations` лежат SQL-файлы. Применяйте их по порядку:

```
cd backend
psql "$DATABASE_URL" -f migrations/001_parent_child_schema.sql
```

Схема включает таблицы `parents`, `children`, `sessions`, `attempts` и чистит старые таблицы прошлого личного кабинета (`users`, `children`, `session_stats`, `attempts`).

📡 Основные API-эндпоинты
Метод	Путь	Описание
POST	/register	Регистрация пользователя
POST	/login	Авторизация
POST	/generate-task	Генерация новой задачи
POST	/check-answer	Проверка ответа
POST	/save-session	Сохранение результата
GET	/last-session-stats	Получить статистику по последней сессии

### Авторизация родителя
Основной аккаунт теперь — родитель. Дети создаются внутри его кабинета (через отдельные эндпоинты управления детьми). Новые маршруты авторизации родителя:

- `POST /auth/register-parent` — регистрация родителя (email, пароль, опционально имя/телефон), возвращает JWT c `parent_id` и `role="parent"`.
- `POST /auth/login-parent` — логин родителя по email/паролю, возвращает JWT c `parent_id` и `role="parent"`.
- `GET /auth/me` — профиль текущего родителя и список его детей (нужен заголовок `Authorization: Bearer <token>`).

🎯 Цели проекта

Создать игровую платформу для обучения детей математике

Внедрить систему монет/мотивации

Добавить родительский кабинет

Разработать персональные тренировочные планы при помощи AI

Готово 🔥
Это уже выглядит как проработанный стартап.
